<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OSMAP | Undergoing Maintenance</title>
    <style>
        .p1 {
            font-family: "Sans-Serif", Arial, serif;
        }

        .p2 {
            font-family: Arial, Helvetica, sans-serif;
        }

        .p3 {
            font-family: "Lucida Console", "Courier New", monospace;
        }
        .p4 {
            font-family: "Lucida Console", "Courier New", monospace;
            color:#fff
        }

        /* Added CSS for button */
        .teacher-area-button {
            position: fixed;
            top: 20px;
            left: 1300px;
            padding: 10px 20px;
            background-color: #ff0000;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        .student-area-button {
            position: fixed;
            top: 20px;
            left: 1150px;
            padding: 10px 20px;
            background-color: #0ac300;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <h1>Error 2: Undergoing Maintenance</h1>
    <h2>We're currently undergoing maintenance. Please try again later </h2>
    <h3> OSMAP ERROR CODES </h3>
    <p> Error 1: Unknown Error
    Error 2: Undergoing Maintenance - no further information
    Error 3: Undergoing Maintenance - Scheduled downtime
    Error 4: Undergoing Maintenance - Update
    Error 5 - Undergoing Maintenance - Short patch downtime
    Error 6: Specific feature downtime
    
    Error 600: Error retrieving L1
    Error 601: Error assigning L1
    Error 602: Student Portal Error
    Error 603: Teacher Portal error
    Error 403: Restricted
    Error 404: page not found
    Error 503: Forbidden
    Error 999: Internal Server Error
    Github error page: Github Error/OSMAP webmap error
    further error information should be on screen</p>
    <h1 class="p4">.</h1>
    <h1 class="p4">.</h1>
    <h1 class="p4">.</h1>
    <h1 class="p4">.</h1>
    <h1 class="p4">.</h1>
    <h1 class="p4">.</h1>
    <h1 class="p4">.</h1>
    <h1 class="p4">.</h1>
    <h1 class="p4">.</h1>
    <h1 class="p4">.</h1>
    
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background-color: #000;
        }
        canvas {
            border: 1px solid #fff;
        }
    </style>
</head>
<body>
    <canvas id="gameCanvas" width="400" height="400"></canvas>

    <script>
        const canvas = document.getElementById('gameCanvas');
        const ctx = canvas.getContext('2d');

        const gridSize = 20;
        const tileCount = canvas.width / gridSize;

        let snake = [{ x: 10, y: 10 }];
        let direction = { x: 0, y: 0 };
        let food = { x: 15, y: 15 };

        function drawTile(x, y, color) {
            ctx.fillStyle = color;
            ctx.fillRect(x * gridSize, y * gridSize, gridSize, gridSize);
        }

        function drawSnake() {
            snake.forEach(segment => drawTile(segment.x, segment.y, 'lime'));
        }

        function drawFood() {
            drawTile(food.x, food.y, 'red');
        }

        function updateSnake() {
            const head = { x: snake[0].x + direction.x, y: snake[0].y + direction.y };
            snake.unshift(head);

            if (head.x === food.x && head.y === food.y) {
                food = {
                    x: Math.floor(Math.random() * tileCount),
                    y: Math.floor(Math.random() * tileCount),
                };
            } else {
                snake.pop();
            }
        }

        function checkCollision() {
            const head = snake[0];

            if (head.x < 0 || head.x >= tileCount || head.y < 0 || head.y >= tileCount) {
                resetGame();
            }

            for (let i = 1; i < snake.length; i++) {
                if (snake[i].x === head.x && snake[i].y === head.y) {
                    resetGame();
                }
            }
        }

        function resetGame() {
            snake = [{ x: 10, y: 10 }];
            direction = { x: 0, y: 0 };
            food = { x: 15, y: 15 };
        }

        function gameLoop() {
            ctx.clearRect(0, 0, canvas.width, canvas.height);

            updateSnake();
            checkCollision();
            drawSnake();
            drawFood();
        }

        document.addEventListener('keydown', event => {
            switch (event.key) {
                case 'ArrowUp':
                    if (direction.y === 0) direction = { x: 0, y: -1 };
                    break;
                case 'ArrowDown':
                    if (direction.y === 0) direction = { x: 0, y: 1 };
                    break;
                case 'ArrowLeft':
                    if (direction.x === 0) direction = { x: -1, y: 0 };
                    break;
                case 'ArrowRight':
                    if (direction.x === 0) direction = { x: 1, y: 0 };
                    break;
            }
        });

        setInterval(gameLoop, 100);
    </script>
</body>
    <footer class="p1">OSMAP, Online School Managment & Administration Platform, AINAPPS, OSMAP APP are property of "Giorgio Valdman"</footer>

</body>
</html>
